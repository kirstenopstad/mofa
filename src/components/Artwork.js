import React from "react";

const Artwork = () => {
  return(
    // add seed data to display artwork detail
    <React.Fragment>
      <img src="#" alt="prompt"/>
      
    </React.Fragment>
  );
}

export default Artwork;